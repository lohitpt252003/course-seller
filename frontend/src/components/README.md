# UI Components

Reusable React components used throughout the application.

- **`Header/`**: Navigation bar with authentication links and theme toggle.
- **`Footer/`**: Site footer.
- **`CourseCard/`**: Displays course summary in listings.
- **`LessonComposer/`**: Reusable teacher authoring builder for lessons, DPPs, quizzes, and assignments with type-specific forms.
- **`ProtectedRoute/`**: Higher-Order Component (HOC) to restrict access to authenticated routes based on user roles.
