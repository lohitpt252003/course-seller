# Context Providers

React Context API usage for global state management.

- **`AuthContext.jsx`**: Manages user authentication state (login, logout, user profile).
- **`ThemeContext.jsx`**: Handles theme switching (light/dark mode).
